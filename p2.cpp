#include <vector>
#include <iostream>
#include <string>


// nao ligar muito para o codigo, so para testar o git mesmo

// Vertex struct
typedef struct {
    int id;
    bool visited;
    std::vector<int> edges;
} vertex;

int main(){
    return 0;
}